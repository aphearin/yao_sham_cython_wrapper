# standalone_cython_sham_wrapper

To build the code:
$ cd sham_wrapper/AbundanceMatching
$ python setup.py build_ext --inplace

Mac users may need the following alternative:
$ CC=clang python setup.py build_ext --inplace

